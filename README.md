
  # design Intranet Layout

  This is a code bundle for design Intranet Layout. The original project is available at https://www.figma.com/design/KcSeS1VeZRbO1YcyAFFcnc/design-Intranet-Layout.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  